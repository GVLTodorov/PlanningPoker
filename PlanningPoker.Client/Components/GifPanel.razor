@if (!string.IsNullOrEmpty(GifUrl))
{
    <aside class="gif-panel" aria-live="polite">
        <img src="@GifUrl" alt="Celebration gif" />
    </aside>
}

@code {
    [Parameter]
    public string? GifUrl { get; set; }
}
